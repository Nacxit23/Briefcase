import React from "react";
import myWorkStyle from "./listMyWork.module.css";
import flexGrid from "../../pages/globalStyle/flexboxgrid.min.module.css"

const listMyWork = props => (
   <div className={`${flexGrid.containerFluid}`}>
    <div className={`${flexGrid.colMd}`}>

    </div>
   </div>
)

export default listMyWork;